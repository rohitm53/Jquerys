$(() => {
  // $("li").replaceWith("<li>Replaced with new </li>");
  // var firstListItem = $("li:first");
  // $("p:first").replaceWith(firstListItem);

  $(".red-box, .blue-box").replaceWith(
    "<div class='green-box'>More green</div>"
  );
});
